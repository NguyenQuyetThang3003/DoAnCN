@model IEnumerable<WedNightFury.Models.Order>
@using System.Globalization
@using System.Text.Json
@using System.Text.RegularExpressions

@{
    ViewData["Title"] = "Lộ trình hôm nay";
    Layout = "~/Views/Shared/_LayoutDriver.cshtml";

    var orders = (Model ?? Enumerable.Empty<WedNightFury.Models.Order>()).ToList();

    string NormalizeAddressLight(string raw)
    {
        var addr = (raw ?? "").Trim();
        if (string.IsNullOrWhiteSpace(addr)) return "";

        addr = Regex.Replace(addr, @"\s+", " ").Trim();
        var lower = addr.ToLowerInvariant();
        if (!lower.Contains("việt nam") && !lower.Contains("viet nam"))
            addr += ", Việt Nam";

        return addr;
    }

    string BuildStopLatLngOnly(WedNightFury.Models.Order o)
    {
        if (o.Lat.HasValue && o.Lng.HasValue)
            return o.Lat.Value.ToString(CultureInfo.InvariantCulture) + "," +
                   o.Lng.Value.ToString(CultureInfo.InvariantCulture);
        return "";
    }

    var stopMap = orders.ToDictionary(o => o.Id, o => BuildStopLatLngOnly(o));
    var stopMapJson = JsonSerializer.Serialize(stopMap);

    int activeIndex = 1;
    var hubOrigin = (ViewBag.HubLatLng as string) ?? "";
}

<div class="driver-route container py-4">
    <h3 class="fw-bold mb-3">
        📍 Lộ trình giao hàng hôm nay
        <span class="badge bg-success ms-2">Gợi ý thứ tự</span>
    </h3>

    @if (!orders.Any())
    {
        <div class="alert alert-info">Hôm nay bạn chưa có đơn nào cần giao.</div>
    }
    else
    {
        <form id="antiForgeryForm" method="post" style="display:none;">
            @Html.AntiForgeryToken()
        </form>

        <!-- ACTION BAR -->
        <div class="top-actions mb-3">
            <div class="left">
                <div class="form-check">
                    <input class="form-check-input" type="checkbox" id="chkAll" />
                    <label class="form-check-label">Chọn tất cả</label>
                </div>
                <span class="text-muted">Đã chọn: <b id="selectedCount">0</b></span>
            </div>

            <div class="right">
                <button type="button" id="btnMapsBatch" class="btn btn-outline-dark btn-sm">
                    🗺 Mở Google Maps
                </button>
                <button type="button" id="btnStartMulti" class="btn btn-primary btn-sm">
                    🚀 Bắt đầu giao
                </button>

                <div class="form-check ms-2">
                    <input class="form-check-input" type="checkbox" id="chkOptimize" />
                    <label class="form-check-label text-muted">Tối ưu lộ trình</label>
                </div>
            </div>
        </div>

        <!-- ORDERS -->
        @foreach (var o in orders)
        {
            bool isActive = o.Status == "pending" || o.Status == "assigned" || o.Status == "shipping";
            bool isFirst = isActive && activeIndex == 1;

            var stopLatLng = BuildStopLatLngOnly(o);
            var addressText = NormalizeAddressLight(o.ReceiverAddress);

            string singleMapUrl =
                !string.IsNullOrWhiteSpace(stopLatLng)
                    ? "https://www.google.com/maps/dir/?api=1&origin=My+Location&destination=" + Uri.EscapeDataString(stopLatLng)
                    : "https://www.google.com/maps/search/?api=1&query=" + Uri.EscapeDataString(addressText);

            <div class="order-card @(isFirst ? "suggested" : "")">
                <div class="header">
                    <div class="left">
                        <div class="order-number @(isActive ? "" : "inactive")"
                             style="@(isFirst ? "background:#28a745" : "")">
                            @(isActive ? activeIndex : "-")
                        </div>

                        <input type="checkbox"
                               class="form-check-input order-select ms-2"
                               value="@o.Id"
                               @(isActive ? "" : "disabled") />

                        <h5 class="mb-0 ms-2">
                            @o.Code
                            @if (isFirst)
                            {
                                <span class="badge bg-success ms-2">Gợi ý</span>
                            }
                        </h5>
                    </div>

                    <span class="badge bg-secondary">@o.Status?.ToUpper()</span>
                </div>

                <div class="row mt-3">
                    <div class="col-md-6">
                        <p><b>👤 Người nhận:</b> @o.ReceiverName</p>
                        <p><b>📞 SĐT:</b> @o.ReceiverPhone</p>
                        <p><b>📍 Địa chỉ:</b> @o.ReceiverAddress</p>

                        @if (!string.IsNullOrWhiteSpace(stopLatLng))
                        {
                            <p class="text-muted mb-0"><b>🧭 Stop:</b> @stopLatLng</p>
                        }
                        else
                        {
                            <p class="text-danger mb-0"><b>🧭 Stop:</b> Chưa có tọa độ</p>
                        }
                    </div>
                    <div class="col-md-6">
                        <p><b>📦 Hàng:</b> @o.ProductName</p>
                        <p><b>⚖ Cân nặng:</b> @o.Weight kg</p>
                        <p><b>💰 COD:</b> @o.CodAmount.ToString("N0") đ</p>
                    </div>
                </div>

                <div class="actions mt-3">
                    <a asp-controller="Taixe"
                       asp-action="StopDetail"
                       asp-route-id="@o.Id"
                       class="btn btn-sm btn-primary">
                        🚀 Giao
                    </a>

                    <a class="btn btn-sm btn-outline-dark"
                       target="_blank"
                       href="@singleMapUrl">
                        🗺 Bản đồ
                    </a>

                    <a asp-controller="Taixe"
                       asp-action="StopDetail"
                       asp-route-id="@o.Id"
                       class="btn btn-sm btn-outline-secondary">
                        📄 Chi tiết
                    </a>
                </div>
            </div>

            if (isActive) { activeIndex++; }
        }
    }
</div>

@section Scripts {
<script>
    const STOP_MAP = @Html.Raw(stopMapJson);
    const HUB_ORIGIN = "@hubOrigin";

    function getToken() {
        return document.querySelector('#antiForgeryForm input[name="__RequestVerificationToken"]')?.value || "";
    }

    function getSelectedIds() {
        return Array.from(document.querySelectorAll(".order-select:checked"))
            .map(x => parseInt(x.value)).filter(x => !isNaN(x));
    }

    function refreshSelectedCount() {
        document.getElementById("selectedCount").innerText = getSelectedIds().length;
    }

    document.querySelectorAll(".order-select").forEach(x =>
        x.addEventListener("change", refreshSelectedCount)
    );

    document.getElementById("chkAll")?.addEventListener("change", function () {
        document.querySelectorAll(".order-select:not(:disabled)")
            .forEach(x => x.checked = this.checked);
        refreshSelectedCount();
    });
</script>
}

<style>
    .driver-route { font-family: "Segoe UI", system-ui, sans-serif; }

    .top-actions {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 12px;
        background: #fff;
        border-radius: 12px;
        border: 1px solid #e5e7eb;
        gap: 12px;
        flex-wrap: wrap;
    }

    .top-actions .left,
    .top-actions .right {
        display: flex;
        align-items: center;
        gap: 12px;
    }

    .order-card {
        background: #fff;
        border-radius: 12px;
        padding: 16px;
        margin-bottom: 16px;
        border: 1px solid #e5e7eb;
        box-shadow: 0 6px 18px rgba(0,0,0,.06);
    }

    .order-card.suggested {
        border-color: #28a745;
    }

    .order-card .header {
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .order-card .header .left {
        display: flex;
        align-items: center;
    }

    .order-number {
        width: 40px;
        height: 40px;
        border-radius: 50%;
        background: #0d6efd;
        color: #fff;
        display: flex;
        align-items: center;
        justify-content: center;
        font-weight: 700;
    }

    .order-number.inactive {
        background: #6c757d;
    }

    .actions {
        display: flex;
        gap: 8px;
        flex-wrap: wrap;
    }

    /* 🔒 Razor-safe media query */
    @@media (max-width: 576px) {
        .top-actions {
            flex-direction: column;
            align-items: stretch;
        }
    }
</style>
